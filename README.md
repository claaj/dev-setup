# Dev setup

This repo contains a my `neovim` and `tmux` configs that I use for development.

The idea of this repo is to be able to install my setup in any linux distro.

Also has a fontalias to set `Adwaita Mono Nerd Font` as the default Monospace font.

## Setup
~~~
./install-all.sh
~~~
